<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Ansi 0 Color</key>
	{{ light.shadowColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Ansi 0 Color (Dark)</key>
	{{ dark.highlightColor | opaque(dark.textBackgroundColor) | format | safe }}

	<key>Ansi 0 Color (Light)</key>
	{{ light.shadowColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Ansi 1 Color</key>
	{{ light.systemRed | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 1 Color (Dark)</key>
	{{ dark.systemRed | format | safe }}

	<key>Ansi 1 Color (Light)</key>
	{{ light.systemRed | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 2 Color</key>
	{{ light.systemGreen | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 2 Color (Dark)</key>
	{{ dark.systemGreen | format | safe }}

	<key>Ansi 2 Color (Light)</key>
	{{ light.systemGreen | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 3 Color</key>
	{{ light.systemBrown | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 3 Color (Dark)</key>
	{{ dark.systemYellow | format | safe }}

	<key>Ansi 3 Color (Light)</key>
	{{ light.systemBrown | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 4 Color</key>
	{{ light.systemBlue | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 4 Color (Dark)</key>
	{{ dark.systemBlue | format | safe }}

	<key>Ansi 4 Color (Light)</key>
	{{ light.systemBlue | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 5 Color</key>
	{{ light.systemPurple | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 5 Color (Dark)</key>
	{{ dark.systemPurple | format | safe }}

	<key>Ansi 5 Color (Light)</key>
	{{ light.systemPurple | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 6 Color</key>
	{{ light.systemTeal | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 6 Color (Dark)</key>
	{{ dark.systemTeal | format | safe }}

	<key>Ansi 6 Color (Light)</key>
	{{ light.systemTeal | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 7 Color</key>
	{{ light.highlightColor | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 7 Color (Dark)</key>
	{{ dark.shadowColor | format | safe }}

	<key>Ansi 7 Color (Light)</key>
	{{ light.highlightColor | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 8 Color</key>
	{{ light.shadowColor | opaque | format | safe }}

	<key>Ansi 8 Color (Dark)</key>
	{{ dark.highlightColor | mix(light.shadowColor, 0.85) | format | safe }}

	<key>Ansi 8 Color (Light)</key>
	{{ light.shadowColor | opaque | format | safe }}

	<key>Ansi 9 Color</key>
	{{ light.systemRed | format | safe }}

	<key>Ansi 9 Color (Dark)</key>
	{{ dark.systemRed | mix(dark.highlightColor, 0.85) | format | safe }}

	<key>Ansi 9 Color (Light)</key>
	{{ light.systemRed | format | safe }}

	<key>Ansi 10 Color</key>
	{{ light.systemGreen | format | safe }}

	<key>Ansi 10 Color (Dark)</key>
	{{ dark.systemGreen | mix(dark.highlightColor, 0.85) | format | safe }}

	<key>Ansi 10 Color (Light)</key>
	{{ light.systemGreen | format | safe }}

	<key>Ansi 11 Color</key>
	{{ light.systemYellow | format | safe }}

	<key>Ansi 11 Color (Dark)</key>
	{{ dark.systemYellow | mix(dark.highlightColor, 0.85) | format | safe }}

	<key>Ansi 11 Color (Light)</key>
	{{ light.systemYellow | format | safe }}

	<key>Ansi 12 Color</key>
	{{ light.systemBlue | format | safe }}

	<key>Ansi 12 Color (Dark)</key>
	{{ dark.systemBlue | mix(dark.highlightColor, 0.85) | format | safe }}

	<key>Ansi 12 Color (Light)</key>
	{{ light.systemBlue | format | safe }}

	<key>Ansi 13 Color</key>
	{{ light.systemPurple | format | safe }}

	<key>Ansi 13 Color (Dark)</key>
	{{ dark.systemPurple | mix(dark.highlightColor, 0.85) | format | safe }}

	<key>Ansi 13 Color (Light)</key>
	{{ light.systemPurple | format | safe }}

	<key>Ansi 14 Color</key>
	{{ light.systemTeal | format | safe }}

	<key>Ansi 14 Color (Dark)</key>
	{{ dark.systemTeal | mix(dark.highlightColor, 0.85) | format | safe }}

	<key>Ansi 14 Color (Light)</key>
	{{ light.systemTeal | format | safe }}

	<key>Ansi 15 Color</key>
	{{ light.highlightColor | format | safe }}

	<key>Ansi 15 Color (Dark)</key>
	{{ dark.shadowColor | mix(dark.highlightColor, 0.85) | opaque | format | safe }}

	<key>Ansi 15 Color (Light)</key>
	{{ light.highlightColor | format | safe }}

	<key>Background Color</key>
	{{ light.textBackgroundColor | format | safe }}

	<key>Background Color (Dark)</key>
	{{ dark.textBackgroundColor | format | safe }}

	<key>Background Color (Light)</key>
	{{ light.textBackgroundColor | format | safe }}

	<key>Badge Color</key>
	{{ light.tertiaryLabelColor | format | safe }}

	<key>Badge Color (Dark)</key>
	{{ dark.tertiaryLabelColor | format | safe }}

	<key>Badge Color (Light)</key>
	{{ light.tertiaryLabelColor | format | safe }}

	<key>Bold Color</key>
	{{ light.secondaryLabelColor | opaque | format | safe }}

	<key>Bold Color (Dark)</key>
	{{ dark.secondaryLabelColor | opaque | format | safe }}

	<key>Bold Color (Light)</key>
	{{ light.secondaryLabelColor | opaque | format | safe }}

	<key>Cursor Color</key>
	{{ light.keyboardFocusIndicatorColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Cursor Color (Dark)</key>
	{{ dark.keyboardFocusIndicatorColor | opaque(dark.textBackgroundColor) | format | safe }}

	<key>Cursor Color (Light)</key>
	{{ light.keyboardFocusIndicatorColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Cursor Guide Color</key>
	{{ light.keyboardFocusIndicatorColor | alpha(0.1) | format | safe }}

	<key>Cursor Guide Color (Dark)</key>
	{{ dark.keyboardFocusIndicatorColor | alpha(0.1) | format | safe }}

	<key>Cursor Guide Color (Light)</key>
	{{ light.keyboardFocusIndicatorColor | alpha(0.1) | format | safe }}

	<key>Cursor Text Color</key>
	{{ light.textColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Cursor Text Color (Dark)</key>
	{{ dark.textColor | opaque(dark.textBackgroundColor) | format | safe }}

	<key>Cursor Text Color (Light)</key>
	{{ light.textColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Foreground Color</key>
	{{ light.textColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Foreground Color (Dark)</key>
	{{ dark.textColor | opaque(dark.textBackgroundColor) | format | safe }}

	<key>Foreground Color (Light)</key>
	{{ light.textColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Link Color</key>
	{{ light.linkColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Link Color (Dark)</key>
	{{ dark.linkColor | opaque(dark.textBackgroundColor) | format | safe }}

	<key>Link Color (Light)</key>
	{{ light.linkColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Selected Text Color</key>
	{{ light.selectedTextColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Selected Text Color (Dark)</key>
	{{ dark.selectedTextColor | opaque(dark.textBackgroundColor) | format | safe }}

	<key>Selected Text Color (Light)</key>
	{{ light.selectedTextColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Selection Color</key>
	{{ light.selectedTextBackgroundColor | opaque(light.textBackgroundColor) | format | safe }}

	<key>Selection Color (Dark)</key>
	{{ dark.selectedTextBackgroundColor | opaque(dark.textBackgroundColor) | format | safe }}

	<key>Selection Color (Light)</key>
	{{ light.selectedTextBackgroundColor | opaque(light.textBackgroundColor) | format | safe }}

</dict>
</plist>
